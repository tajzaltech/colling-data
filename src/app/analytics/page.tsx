"use client";

import { AnalyticsView } from "@/components/views/AnalyticsView";

export default function AnalyticsPage() {
    return (
        <div style={{
            minHeight: '100vh',
            background: '#ffffff',
            padding: '2rem'
        }}>
            <AnalyticsView />
        </div>
    );
}
