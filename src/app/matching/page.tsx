"use client";

import { MatchingView } from "@/components/views/MatchingView";

export default function MatchingPage() {
    return (
        <div style={{
            minHeight: '100vh',
            background: '#ffffff',
            padding: '2rem'
        }}>
            <MatchingView />
        </div>
    );
}
